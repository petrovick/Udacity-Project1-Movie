<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:background">@android:color/transparent</item>
    </style>
    
    
    <style name="CustomTextViewTheme">
        <item name="android:textSize">@dimen/textViewSize</item>
        <item name="android:layout_marginLeft">@dimen/marginLeft</item>
        <item name="android:textColor">@color/darkerBlack</item>
        <!--<item name="android:textColor">@color/pretoClaro</item>-->
    </style>

</resources>
